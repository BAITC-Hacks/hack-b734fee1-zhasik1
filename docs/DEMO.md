# QOR demo (3-5 minutes)

> For the implemented `frontend/app.py` entry point, exact current UI labels
> and honest real-IEK/synthetic-SE sequence, use the short judge demo in
> [Current implementation evidence](IMPLEMENTATION_VERIFICATION.md).

1. Start the app and show the 22 Sep 2026 snapshot badge and truthful source
   setup state. Upload the supplied archives when available.
2. On Data quality, show source rows, normalized columns, missing fields, and
   that a textual SKU remains textual.
3. On Demand & outliers, inspect a candidate large transaction. The built-in
   walkthrough is synthetic and visibly labeled; it is not supplier evidence.
4. In Order drafts, set 14-day lead time, open the dated inventory timeline,
   adjust one recommended quantity with a reason, then move draft -> review ->
   approved and export CSV/XLSX. No supplier is contacted.
5. Compare lead-time scenarios (7/14/30). Show `MODEL_ACCESS.md`: AI call is
   pending until organizer access is supplied; do not demonstrate a mock.
